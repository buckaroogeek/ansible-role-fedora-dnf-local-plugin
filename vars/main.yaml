---
# vars file for fedora_dnf_local_plugin

# packages to install
dlp_packages:
  - python3-dnf-plugin-local
  - createrepo_c

# configuration file path; assumed to be ini format
dlp_conf: /etc/dnf/plugins/local.conf
...
